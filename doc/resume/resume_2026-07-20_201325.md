# Resume prompt — 2026-07-20 20:13:25

You are picking up work on **goldberg-system**, the code repository for the document analysis platform. This prompt hands off from the session that created the four repos and bootstrapped this one.

## What this platform is

A pipeline that turns raw legal documents into a searchable, LLM-queryable knowledge base, running on **Halob** (home NAS, `192.168.86.31`). It replaces a single legacy repo (`the_goldberg_files`) that mixed original data, extractions, and code. Read, in order:
1. `README.md`
2. `doc/architecture.md` — the four-repo topology + Halob infra + Mind of Steele reuse
3. `doc/workflow.md` — the ingestion pipeline (what happens to a document)
4. `config/projects.yaml` — where every repo and service lives (single source of truth)

## The four repos (all private, on GitHub under the project owner's account, checked out under `/Volumes/Home/work/project_goldberg/`)

- **goldberg-system** (this) — the pipeline & tooling
- **goldberg-raw** — immutable originals; push triggers the pipeline
- **goldberg-extracted** — machine-generated markdown + metadata (regenerable)
- **goldberg-casework** — authored work product (briefings, applications, analysis)

The legacy repo `the_goldberg_files` (Dropbox path in `config/projects.yaml → archive`) is **FROZEN** — do not modify; it is the rollback point.

## State at hand-off (done)

- Four private GitHub repos created and checked out on Halob under a common parent.
- This repo initialised with **Spec Kitty** (`.kittify/`, `AGENTS.md`, `auto_commit: true`) by the user, then bootstrapped: `pyproject.toml` (uv), `src/goldberg_system/` (config loader + CLI), `tests/` (TDD), `config/projects.yaml`, and `doc/`.
- Elasticsearch on Halob already holds the *legacy* content (index `goldberg_files`, ~589 docs) from the frozen repo.
- Mind of Steele (`~/workspace/mind_of_steele`) is confirmed as the reference implementation to reuse (`llm_support`, `elasticsearch/`, `ragie_uploader`, `live_summarize`).

## What's next (build order)

1. **Pipeline modules** (TDD — tests first, mandatory):
   - trigger listener (NATS `goldberg.raw.ingested`) — service loop modelled on MoS `live_summarize/main.py`
   - extractors (dispatch by mime: pdftotext / tesseract OCR / eml-to-md / pandoc / readability / passthrough)
   - enrich (reuse MoS `common.llm_support`: summary + keywords + YAML frontmatter)
   - sinks behind an interface: goldberg-extracted writer, Elasticsearch indexer (MoS `elasticsearch/`), wiki sink
2. **Trigger** on the Halob `goldberg-raw` clone (hook or watcher → NATS). NOTE: origin is GitHub, which has no server-side hooks — use a Halob-local hook/watcher, or a GitHub-webhook → Halob receiver if content is committed off-Halob.
3. **Deploy** the `live-index` service (docker-compose, à la MoS).
4. **Migrate** off the frozen archive: originals → goldberg-raw; authored work → goldberg-casework; regenerate extractions → goldberg-extracted (backfill).
5. **Backfill + verify** ES coverage.

## Open decisions (unresolved)

- **Wiki sink**: Ragie (managed RAG, reuse MoS uploader) vs Obsidian vault vs RAG-on-Elasticsearch.
- **Trigger locus**: Halob-local hook (recommended) vs GitHub webhook.
- **Large binaries** in goldberg-raw: plain git vs git-LFS.

## Constraints / conventions

- **TDD is mandatory** — write failing tests before implementation.
- **uv** for dependency management (`uv sync`, `uv run pytest`).
- **Reuse Mind of Steele**, do not reinvent; resolve it as a git source / vendored package (it's on the Mac, not Halob).
- Repos are **private** and hold **legally sensitive** material (CPIA s.17 disclosure, personal data, and material obtained via unofficial channels in one of the parallel matters) — keep private; be deliberate about what goes to GitHub vs Halob-only.
- **Provenance** matters (legal case): every derived doc links back to its raw source + commit.
- Spec Kitty is present and `auto_commit: true` — be aware it may commit changes; mission-style work routes through `/spec-kitty.specify`.

## Wider context (not this repo's job, but why it exists)

This platform supports live litigation research and drafting for its owner. It is
infrastructure to enable large-scale analysis of case material (e.g. the deferred
rigorous claim-vs-evidence analysis). The actual legal work lives in
`goldberg-casework` and the frozen archive — **not** here. Do not let platform work
disrupt live case prep.
