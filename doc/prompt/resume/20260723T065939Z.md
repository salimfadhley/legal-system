# Resume prompt — goldberg-system (`system` agent)

Continuation prompt for a fresh session. Written 2026-07-23T06:59:39Z. You are the
**`system`** agent (technical/infra lane) for the Goldberg legal-corpus project.
Read this, then pick up where it says.

## 0. FIRST THING — bootstrap agent-mail (the reason this session likely restarted)

This session was restarted to load the **agent-mail** MCP tools, which were registered
mid-session and only appear after a reload. On startup:

1. Confirm the tools now exist (`ping`, `hub_info`, `check_inbox`, `read_message`,
   `reply_message`, `send_message`, `notify_agent`). If they're present, call **`ping`**
   → expect `{ ok: true, agent: "goldberg_system/opus", … }`. Then `hub_info`, then
   `check_inbox`, and report one line:
   `agent-mail: connected as goldberg_system/opus on hub homelab — N message(s) waiting.`
2. **If the tools are still absent**, they didn't load — tell the human; do not pretend
   mail works. Fallback that DOES work without the tools: drive the endpoint over raw
   JSON-RPC (initialize → capture `mcp-session-id` → `notifications/initialized` →
   `tools/call`). A working example script is
   `…/scratchpad/mail_poll_30m.sh` (from the prior session) if still present.

**agent-mail facts:**
- My identity/endpoint: **`http://halob:8080/goldberg_system/opus/mcp`** (project
  `goldberg_system`, agent `opus`). Registered in Claude Code local config
  (`claude mcp list` → agent-mail ✔ Connected).
- Hub: **`homelab`** at `http://halob:8080` (health `/health`, info `/`). Server
  `agent-mail` v1.28.1. Admin agent addressed as `agent_mail/admin`. Issues:
  `github.com/salimfadhley/agent-mail`.
- Addressing: `project/agent` (direct) · `project` (any one) · `project/*` (broadcast).
  Only agents in project **`goldberg_system`** are reachable by my bare/broadcast forms.
- **Habit:** call `check_inbox` at the START of every turn; surface mail before continuing.

**Outstanding mail thread:** I sent a connectivity test to `agent_mail/admin` —
thread/id **`d704ab1e3f3a40ff87d2756c1837f4fb`** (sent 2026-07-23T06:25:44Z), asking
admin to confirm the return path. **No reply had arrived** as of this handoff. Check
inbox; if admin replied, read it and tell the human the round-trip is proven.

**The 30-minute poller is dead** if the session restarted (it was a persistent Monitor,
`scratchpad/mail_poll_30m.sh`, polling my inbox every 1800s and notifying only on NEW
mail via a seen-id file). With native tools loaded you don't need it — `check_inbox`
each turn replaces it. Only re-launch it if the human wants passive between-turn alerts.

## 1. Just-completed work (durability + privacy) — DONE, for context

casework asked `system` (via git-ledger thread `20260722T213950Z_durability-and-privacy`)
to guarantee the repos stay private and no case data is lost. Completed and `actioned`:
- **All four repos are PRIVATE** on GitHub (`goldberg-casework`, `goldberg-system`,
  `goldberg-raw`, `goldberg-extracted`). Nothing in the pipeline can flip visibility.
- **`goldberg-system`**: pushed `main` + `feat/goldberg-nats-es-archive` (was local-only).
- **`goldberg-raw`** (evidence): pushed a **de-LFS'd plain-git recovery snapshot** of the
  entire current corpus (1,919 files, ~957 MB, largest 58 MB) to the private remote
  (`main` = `2acf2ff`), carrying `BACKUP_MANIFEST.md`. This excludes only the ~4.7 GB of
  already-deleted movie/audio (in the frozen archive) + the LFS history. The working
  repo's LFS history was **NOT** rewritten (pipeline provenance `raw_commit` depends on
  it). The snapshot is a separate backup lineage; refresh by re-running the rsync→init→
  push in `scratchpad/goldberg-raw-snapshot`.

## 2. Inter-agent comms protocol (git-ledger, v2)

Full rules in global `~/.claude/CLAUDE.md` → "Inter-Agent Messaging". Own-repo-only;
git commits are the ack ledger; turns live in `doc/prompt/<thread-id>/<ts>_<from>.md`
with `intent: message|reply|ack|actioned`. To find work addressed to me, read
**casework's** repo (`../goldberg-casework/doc/prompt/`) for `to: system` turns.
- **Still awaiting casework:** a `reply` adopting the protocol on thread
  `20260722T194900Z_comms-protocol-v2` (in casework's repo). Check for it.
- NOTE: agent-mail (above) is the newer live channel; the git-ledger is the durable/
  interim one. Both exist; don't confuse them.

## 3. Open technical follow-ups (offered to human; none started)
- **Privacy monitor** — scheduled `gh repo view … visibility` alarm if any repo becomes
  non-PRIVATE.
- **Snapshot-refresh job** — keep `goldberg-raw`'s recovery snapshot current with new
  evidence (today it's point-in-time at working HEAD).
- Optional **second mirror** (DO Spaces) for two independent durable copies.
- Cosmetic **`mcp_server` self-probe** fix in doctor (misreports DOWN when self-probing).
- Convert directly-run Halob containers into a managed **Portainer stack**
  (`docker-compose.yml` ready). **selftest** mission (specced, not built).
- **nats-es-archive** mission (`kitty-specs/nats-es-archive-01KY5K1J`, deferred future).

## 4. Ground rules
- Ground factual case claims via the **goldberg** query tools / MCP (`find_claims`,
  `search_evidence`, `search_concepts`, `get_document`); cite `doc_id + raw_path +
  speaker + date`. Never invent.
- Stay in the **technical lane**; route legal work to casework.
- Git preference: auto-merge to `main`, no PRs unless warned, move fast. UTC everywhere.
- **agent-mail is a separate handed-off project** (`/Volumes/Home/work/agent-mail`,
  owned by another LLM) — I only *use* the hub; I don't build it.
