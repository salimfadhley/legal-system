# Resume prompt — goldberg-system (`system` agent)

Written 2026-07-23T07:03:45Z. Short successor to
[`20260723T065939Z.md`](./20260723T065939Z.md) — **read that one for full context**
(durability/privacy work, comms protocol, open follow-ups, ground rules). This note
only records what changed on the restart it predicted.

## agent-mail bootstrap — CONFIRMED WORKING

The prior resume's Step 0 is done. On this session's startup:
- MCP tools loaded (`ping`, `hub_info`, `check_inbox`, `read_message`, `reply_message`,
  `send_message`, `notify_agent`).
- `ping` → `{ ok: true, agent: "goldberg_system/opus", … }`. **Identity confirmed:**
  project `goldberg_system`, agent `opus`; endpoint
  `http://halob:8080/goldberg_system/opus/mcp`.
- `hub_info` → hub **`homelab`**, version **0.1.0**, `max_message_bytes` 1048576,
  admin `admin`, issues `github.com/salimfadhley/agent-mail/issues`.
  (Note: prior resume cited server "v1.28.1"; `hub_info` now reports hub version 0.1.0
  — cosmetic version-string mismatch, connectivity is fine.)
- `check_inbox` → **empty. 0 messages waiting.**

**Habit going forward:** call `check_inbox` at the START of every turn.

## Still outstanding (unchanged from prior resume)
- **Admin connectivity test** thread `d704ab1e3f3a40ff87d2756c1837f4fb` (sent
  2026-07-23T06:25:44Z to `agent_mail/admin`): **no reply yet** — inbox is empty, so
  the return round-trip from admin is still unproven. Not blocking.
- **casework** has not yet replied on git-ledger thread `20260722T194900Z_comms-protocol-v2`
  (check `../goldberg-casework/doc/prompt/`).

Everything else (open technical follow-ups, ground rules, lane discipline) is as listed
in the prior resume — nothing started this session beyond the bootstrap confirmation.
