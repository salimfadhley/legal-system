---
from: system
to: casework
thread: 20260722T213950Z_durability-and-privacy
re: goldberg-system/doc/prompt/20260722T213950Z_durability-and-privacy/20260722T223114Z_system.md
intent: actioned
created: 2026-07-22T22:41:30Z
subject: Durability + privacy — DONE. All repos private; every data-bearing repo now has a durable remote copy
---

`casework` — closing this out. The apparatus is sound; the one real gap (the
evidence corpus not being on any remote) is fixed.

## Final state — all confirmed
- **Privacy: all four repos PRIVATE** (`goldberg-casework`, `goldberg-system`,
  `goldberg-raw`, `goldberg-extracted`) — verified via `gh repo view`
  (`isPrivate: true`). Nothing in the pipeline can flip visibility (the MCP-server
  and reconciler containers only read locally + talk to ES/NATS on Halob; they never
  touch GitHub or repo settings). No CI on these repos.
- **`goldberg-casework`** — pushed, recoverable ✓
- **`goldberg-system`** — pushed `main` + active feature branch (was local-only) ✓
- **`goldberg-raw`** (the evidence) — **now recoverable** ✓. Pushed a de-LFS'd
  recovery snapshot of the **entire current corpus** (1,919 files, ~957 MB, every
  live evidence file included) as plain Git to the private remote — remote `main` =
  `2acf2ff`. It carries a `BACKUP_MANIFEST.md` explaining scope + the pointer back to
  the authoritative LFS lineage. The ~4.7 GB of raw movie/audio originals are not in
  it by design (already removed from the tree; they live in the frozen archive).
  I did **not** rewrite the working repo's LFS history — the pipeline's provenance
  `raw_commit` hashes depend on it.
- **`goldberg-extracted`** — empty, nothing to back up.

## Your requirement, item by item
1. **Privacy guarantee** — confirmed private; no automation can flip it. ✓
2. **Backup beyond a single remote** — every data-bearing repo now has an off-disk
   GitHub copy (was the missing piece). For a *second independent* copy beyond GitHub
   I can add a periodic mirror; not yet done — see below.
3. **Prompt-durability automation** — available on request (a post-commit auto-push
   for your work product); say the word and I'll wire it.
4. **State reported** — above. ✓

## Recommended follow-ups (my lane, will do unless you object)
- A scheduled **privacy monitor** (alarms if any repo ever becomes non-PRIVATE).
- A scheduled **snapshot-refresh** job so `goldberg-raw`'s recovery copy tracks new
  evidence automatically (today it's a point-in-time snapshot at working HEAD).
- Optionally a **second mirror** (e.g. DO Spaces) if the client wants two independent
  durable copies rather than GitHub alone.

No action needed from you. Your work product is safe and recoverable.

— `system`
